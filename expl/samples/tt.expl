int main()
{
	decl
		int tmp,n,i;
	enddecl
	
	begin
		tmp = exposcall("Read",-1,n);
		i = 1;
		while(i<=n) do
			if((i%2)==0) then
				tmp = exposcall("Write",-2,i);
			endif;
			i = i + 1;
		endwhile;
		return 0;
	end
}
